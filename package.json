{
  "name": "scentoria-monitor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Notifies on new arrivals and restocks at scentoria.co.in via a Telegram bot, on a Cloudflare Worker cron.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node test/monitor.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
